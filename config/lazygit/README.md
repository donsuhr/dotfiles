# LazyGit Notes

add themes file from config/delta to config/delta

update ~/.gitconfig:

```ini
[include]
  path = ~/.config/delta/themes.gitconfig

[delta]
  side-by-side = true
  features = decorations mellow-barbet
  icons = true
  tabs = 2
  ; chameleon | mellow-barbet
```
